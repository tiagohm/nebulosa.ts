name: CI

on:
    push:
        branches: [main]
        paths:
            - 'src/**.ts'
            - 'tests/**.ts'
            - 'native/**'
            - 'package.json'
            - 'tsconfig.json'
            - 'bunfig.toml'
            - '.github/workflows/**'
    pull_request:
        paths:
            - 'src/**.ts'
            - 'tests/**.ts'
            - 'native/**'
            - 'package.json'
    workflow_dispatch:

jobs:
    test:
        name: Test
        strategy:
            matrix:
                os: [ubuntu-24.04, ubuntu-24.04-arm, windows-2025]
        runs-on: ${{ matrix.os }}
        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: Setup Bun
              uses: oven-sh/setup-bun@v2

            - name: Install Dependencies
              run: bun i --omit=dev

            - name: Cache Data Files
              if: ${{ matrix.os == 'ubuntu-24.04' }}
              uses: actions/cache@v5
              with:
                  path: data/
                  key: data-cache-${{ hashFiles('data/.version') }}
                  restore-keys: |
                      data-cache-

            - name: Type Check
              if: ${{ matrix.os == 'ubuntu-24.04' }}
              run: bun tsc

            - name: Run Tests
              if: ${{ matrix.os == 'ubuntu-24.04' }}
              run: bun test --timeout 1000

            - name: Run Tests
              if: ${{ matrix.os != 'ubuntu-24.04' }}
              run: bun test --timeout 1000 wcs.test.ts jpeg.test.ts
